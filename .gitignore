node_modules/
_site/
.DS_Store

# Generated by scripts/sync-projects.sh — committed so builds work without re-syncing,
# but regenerated fresh on each DigitalOcean build.
# src/devlog/          ← do NOT ignore, we want these committed
# src/images/devlog/   ← same
# src/<project-slug>/  ← same
